@model mToDo.Model.ToDoItem

@{
    ViewBag.Title = "New Task";
}

<h2>New Task</h2>

@using (Html.BeginForm()) 
{
    @Html.AntiForgeryToken()
    
    <div class="form-horizontal">
        <h4>ToDoItem</h4>
        <hr />
        @Html.ValidationSummary(true)

        <div class="form-group">
            @Html.LabelFor(model => model.ToDoItemText, new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.EditorFor(model => model.ToDoItemText)
                @Html.ValidationMessageFor(model => model.ToDoItemText)
            </div>
        </div>

        @*<div class="form-group">
            @Html.LabelFor(model => model.ToDoItemIsDone, new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.EditorFor(model => model.ToDoItemIsDone)
                @Html.ValidationMessageFor(model => model.ToDoItemIsDone)
            </div>
        </div>*@

        @Html.HiddenFor(model => model.ToDoItemIsDone) 

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Add New" class="btn btn-default" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>
